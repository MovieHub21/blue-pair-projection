'use client'
import Image from 'next/image'
import Link from 'next/link'
import { useEffect, useMemo, useState } from 'react'
import { useRouter, useSearchParams } from 'next/navigation'
import { ArrowRight, CheckCircle2, Loader2, Users, BedDouble, Ruler, BellRing, Clock3 } from 'lucide-react'
import { naira, todayISO, addDaysISO } from '../../../../../lib/format'
import { useAuth } from '../../../../../lib/useAuth'
import AvailabilityCalendar from '../../../../../components/booking/AvailabilityCalendar'
import type { RoomType } from '../../../../../data/mock'
type Unit = { id:string; room_number:string; name?:string|null; slug:string; image_url?:string|null; images?:string[]|null; floor?:string; status:string }
type GuestStatus = 'available'|'availableSoon'|'taken'|'reserved'
function RoomGallery({ images, name }: { images:string[]; name:string }) { const [selected,setSelected]=useState(0); useEffect(()=>{setSelected(0)},[images.join('|')]); if(images.length===0)return null; const mainImage=images[selected]; const sideImages=images.filter((_,i)=>i!==selected); return <div className="relative min-w-0 max-w-full overflow-hidden"><div className={`grid min-w-0 max-w-full gap-2.5 md:gap-3 ${images.length>1?'md:grid-cols-[1.65fr_1fr]':''}`}><button type="button" onClick={()=>setSelected(selected)} className="group relative min-w-0 w-full h-[360px] sm:h-[420px] md:h-[500px] overflow-hidden rounded-[1.25rem] md:rounded-[1.5rem] border border-gold-500/15 bg-navy-950 shadow-pop text-left" aria-label={`View ${name}, photo ${selected+1}`}><Image src={mainImage} alt={`${name}, photo ${selected+1}`} fill priority className="object-contain md:object-cover transition duration-700 group-hover:scale-[1.025]" sizes="(max-width: 768px) 100vw, 65vw"/><div className="absolute inset-0 bg-gradient-to-t from-navy-950/40 via-transparent to-white/5 pointer-events-none"/><div className="absolute left-3 bottom-3 md:left-4 md:bottom-4 rounded-full border border-white/20 bg-navy-950/70 px-2.5 py-1.5 md:px-3 md:py-1.5 text-[9px] md:text-[10px] font-semibold uppercase tracking-[0.16em] text-white backdrop-blur-md">Photo {selected+1} of {images.length}</div></button>{sideImages.length>0&&<div className="hidden md:grid min-w-0 gap-3 md:grid-rows-2 overflow-hidden">{sideImages.map((image)=>{const actualIndex=images.findIndex(value=>value===image);return <button key={`${image}-${actualIndex}`} type="button" onClick={()=>setSelected(actualIndex)} className="group relative min-w-0 w-full min-h-0 overflow-hidden rounded-[1.25rem] border border-black/10 bg-navy-950 text-left" aria-label={`View ${name}, photo ${actualIndex+1}`}><Image src={image} alt={`${name}, photo ${actualIndex+1}`} fill className="object-cover transition duration-500 group-hover:scale-[1.04]" sizes="35vw"/><div className="absolute inset-0 bg-gradient-to-t from-navy-950/40 via-transparent to-transparent"/><span className="absolute right-3 bottom-3 rounded-full bg-white/90 px-2.5 py-1.5 text-[10px] font-semibold text-navy-950 shadow-sm backdrop-blur-sm">View {actualIndex+1}</span></button>})}</div>}</div>{images.length>1&&<div className="flex md:hidden w-full min-w-0 gap-2 mt-2.5 overflow-hidden">{images.map((image,index)=><button key={`${image}-mobile-thumb-${index}`} type="button" onClick={()=>setSelected(index)} className={`relative min-w-0 flex-1 h-[64px] overflow-hidden rounded-xl border-2 transition ${selected===index?'border-gold-500 ring-2 ring-gold-500/15':'border-transparent opacity-70'}`} aria-label={`Select photo ${index+1}`} aria-pressed={selected===index}><Image src={image} alt={`${name}, photo ${index+1}`} fill className="object-cover" sizes="33vw"/><span className="absolute left-1.5 bottom-1.5 rounded-full bg-navy-950/75 px-1.5 py-0.5 text-[8px] font-semibold text-white backdrop-blur-sm">{index+1}</span></button>)}</div>}{images.length>1&&<div className="hidden md:flex items-center gap-2 mt-3 overflow-x-auto pb-1">{images.map((image,index)=><button key={`${image}-thumb-${index}`} type="button" onClick={()=>setSelected(index)} className={`relative shrink-0 w-16 h-12 sm:w-20 sm:h-14 overflow-hidden rounded-xl border-2 transition ${selected===index?'border-gold-500 ring-2 ring-gold-500/15':'border-transparent opacity-65 hover:opacity-100'}`} aria-label={`Select photo ${index+1}`} aria-pressed={selected===index}><Image src={image} alt="" fill className="object-cover" sizes="80px"/></button>)}<span className="text-[10px] font-medium uppercase tracking-[0.14em] text-navy-400 whitespace-nowrap ml-1">Tap any photo to view</span></div>}</div> }
export default function IndividualRoomClient({ type, unit }: { type: RoomType; unit: Unit }) {
 const router=useRouter(); const params=useSearchParams(); const auth=useAuth(); const [checkIn,setCheckIn]=useState(params.get('checkin')||todayISO()); const [checkOut,setCheckOut]=useState(params.get('checkout')||addDaysISO(1,params.get('checkin')||todayISO())); const [status,setStatus]=useState<GuestStatus|null>(null); const [availableFrom,setAvailableFrom]=useState<string|null>(null); const [paymentReady,setPaymentReady]=useState(false); const [checking,setChecking]=useState(true); const [reserving,setReserving]=useState(false); const [reserved,setReserved]=useState(false); const [error,setError]=useState<string|null>(null)
 const name=unit.name||`Room ${unit.room_number}`; const gallery=useMemo(()=>{const physical=Array.from(new Set([...(unit.images||[]),unit.image_url].filter(Boolean) as string[])).slice(0,3);return physical.length>0?physical:Array.from(new Set((type.images||[]).filter(Boolean))).slice(0,3)},[unit.images,unit.image_url,type.images]); const nights=Math.max(1,Math.round((new Date(checkOut).getTime()-new Date(checkIn).getTime())/86400000)); const total=type.price*nights; const tax=Math.round(total*.075)
 useEffect(()=>{if(!checkIn||!checkOut||checkIn>=checkOut){setChecking(false);setStatus(null);return}let cancelled=false;const load=async()=>{if(!cancelled){setChecking(true);setStatus(null);setAvailableFrom(null);setPaymentReady(false);setReserved(false)}try{const response=await fetch(`/api/public/availability?checkin=${encodeURIComponent(checkIn)}&checkout=${encodeURIComponent(checkOut)}&roomTypeId=${encodeURIComponent(type.id)}&_=${Date.now()}`,{cache:'no-store'});const data=response.ok?await response.json():null;if(cancelled)return;const current=data?.rooms?.find((r:any)=>r.id===unit.id);setStatus(current?.guest_status||null);setAvailableFrom(current?.available_from||null);setPaymentReady(Boolean(current?.payment_ready));setReserved(current?.guest_status==='reserved')}catch{if(!cancelled)setError('Unable to check this room right now.')}finally{if(!cancelled)setChecking(false)}};void load();return()=>{cancelled=true}},[checkIn,checkOut,type.id,unit.id])
 async function reserve(){if(!auth.userId){router.push(`/account/login?redirect=${encodeURIComponent(`/rooms/${type.slug}/${unit.slug}?checkin=${checkIn}&checkout=${checkOut}`)}`);return}setReserving(true);setError(null);try{const response=await fetch('/api/public/reservations',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({roomTypeId:type.id,roomId:unit.id,checkIn,checkOut,adults:2,children:0,amount:total+tax})});const data=await response.json().catch(()=>null);if(!response.ok)throw new Error(data?.error||'Unable to reserve this room right now.');setReserved(true);setStatus('reserved');setPaymentReady(false)}catch(e:any){setError(e?.message||'Unable to reserve this room right now.')}finally{setReserving(false)}}
 const updateDates=(ci:string,co:string)=>{setCheckIn(ci);setCheckOut(co);router.replace(`/rooms/${type.slug}/${unit.slug}?checkin=${encodeURIComponent(ci)}&checkout=${encodeURIComponent(co)}`,{scroll:false})}
 return <div className="container-w min-w-0 max-w-full overflow-x-hidden px-4 sm:px-6 md:px-10 py-8"><Link href={`/rooms/${type.slug}?checkin=${encodeURIComponent(checkIn)}&checkout=${encodeURIComponent(checkOut)}`} className="text-xs text-navy-400 hover:text-navy-700">← Back to {type.name}</Link><div className="grid min-w-0 lg:grid-cols-[1.35fr,.65fr] gap-8 mt-5"><div className="min-w-0 max-w-full"><RoomGallery images={gallery} name={name}/><span className="eyebrow mt-8 inline-block">{type.category} · Room {unit.room_number}</span><h1 className="text-4xl font-semibold mt-2">{name}</h1><p className="text-xs text-navy-400 mt-2">Floor {unit.floor||'—'} · Specific physical room</p><p className="text-navy-500 leading-relaxed mt-4">{type.description}</p><div className="flex flex-wrap gap-8 py-6 my-6 border-y border-black/10"><div className="flex gap-2"><Users size={18} className="text-gold-500"/><b>{type.guests} guests</b></div><div className="flex gap-2"><BedDouble size={18} className="text-gold-500"/><b>{type.bedType}</b></div><div className="flex gap-2"><Ruler size={18} className="text-gold-500"/><b>{type.sizeSqm} m²</b></div></div><AvailabilityCalendar roomId={unit.id} initialCheckIn={checkIn} initialCheckOut={checkOut} onSelect={updateDates}/><h2 className="text-xl font-semibold mt-8 mb-4">Amenities</h2><div className="grid sm:grid-cols-2 gap-3">{type.amenities.map(a=><div key={a} className="flex gap-2 text-sm"><CheckCircle2 size={16} className="text-gold-500"/>{a}</div>)}</div></div><aside className="card p-6 h-fit sticky top-24 min-w-0 max-w-full"><div className="text-xs text-navy-400 mb-2">Room rate</div><b className="font-display text-3xl">{naira(type.price)}</b><span className="text-xs text-navy-400"> / night</span><div className="h-px bg-black/10 my-5"/><label className="field-label">Check-in</label><input type="date" min={todayISO()} value={checkIn} onChange={e=>{const v=e.target.value;setCheckIn(v);if(v>=checkOut)setCheckOut(addDaysISO(1,v))}} className="field-input mb-2"/><p className="text-[10px] text-navy-400 mb-4">Standard arrival: 3:00 PM</p><label className="field-label">Check-out</label><input type="date" min={addDaysISO(1,checkIn)} value={checkOut} onChange={e=>setCheckOut(e.target.value)} className="field-input mb-2"/><p className="text-[10px] text-navy-400 mb-4">Standard departure: 12:00 PM</p><div className="flex justify-between text-sm"><span>{naira(type.price)} × {nights} nights</span><b>{naira(total)}</b></div><div className="flex justify-between text-sm mt-2"><span>Taxes & fees</span><b>{naira(tax)}</b></div><div className="flex justify-between font-semibold border-t border-black/10 mt-4 pt-4"><span>Total</span><b>{naira(total+tax)}</b></div><div className={`mt-5 rounded-lg px-3 py-2.5 text-sm font-semibold ${checking?'bg-navy-50 text-navy-500':status==='available'?'bg-emerald-50 text-emerald-700':status==='availableSoon'?'bg-gold-50 text-gold-700':status==='reserved'?'bg-emerald-50 text-emerald-700':'bg-red-50 text-red-700'}`}>{checking?'Checking availability…':status==='available'?'Available to book':status==='availableSoon'?`Available soon${availableFrom?` · from ${availableFrom}`:''}`:status==='reserved'?(paymentReady?'Available — payment ready':'Reserved for you'):'Already Taken'}</div>{!checking&&status==='available'&&<Link href={`/booking?room=${type.slug}&unit=${unit.id}&checkin=${encodeURIComponent(checkIn)}&checkout=${encodeURIComponent(checkOut)}`} className="btn-primary w-full justify-center mt-5">Book & pay <ArrowRight size={15}/></Link>}{!checking&&status==='availableSoon'&&!reserved&&<button onClick={reserve} disabled={reserving||auth.loading} className="btn-gold w-full justify-center mt-5 disabled:opacity-60">{reserving?<><Loader2 size={15} className="animate-spin"/>Reserving…</>:<>Reserve — pay when available <BellRing size={15}/></>}</button>}{!checking&&status==='reserved'&&paymentReady&&<Link href="/account/bookings" className="btn-gold w-full justify-center mt-5">Pay now <ArrowRight size={15}/></Link>}{!checking&&status==='reserved'&&!paymentReady&&<div className="mt-5 rounded-xl border border-gold-200 bg-gold-50 p-4 text-xs text-gold-800 flex gap-2"><Clock3 size={15} className="shrink-0 mt-0.5"/><span>No payment yet. We’ll email you when this reserved room becomes available, then you can pay to secure it.</span></div>}{!checking&&status==='taken'&&<button type="button" disabled className="btn-primary w-full justify-center mt-5 opacity-60 cursor-not-allowed">Already Taken</button>}{status==='availableSoon'&&!reserved&&<p className="text-[10px] text-navy-400 text-center mt-2">No payment is taken now. You will be emailed when this room becomes available, then payment will secure it.</p>}{error&&<div className="mt-3 rounded-lg bg-red-50 px-3 py-2.5 text-xs text-red-700">{error}</div>}</aside></div></div>
}
